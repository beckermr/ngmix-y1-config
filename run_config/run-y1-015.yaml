# run name
run: 'y1-015'

# versions
meds_version: 'tb-y1a1-s82-000'
nbrs_version: '001'
obj_flags_version: '001'

# job setup
num_fofs_per_chunk: 100
output_dir: '.'

# what to do
bands: ['g','r','i','z']

# how to assemble results
concat_type: 'concat-des'
